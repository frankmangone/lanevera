var $item = $("#item<%= params[:product_id] %>");
var $counter = $item.find(".cart-counter");

var $text = $counter.find("p");
var amount = parseInt($text.text());

//
amount++;
//

$text.text(amount);

var $list_text = $("#list_item<%= params[:product_id] %>").find(".list-amount").find(".value");
$list_text.text(amount);

setTotalPrice();
