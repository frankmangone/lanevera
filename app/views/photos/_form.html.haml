#photo-preview
	%p Click acá para seleccionar una imagen
= form_for @photo, html: { multipart: true }, remote: true do |f|
	= f.label "Foto"

	= f.file_field :photo, class: 'form-control'
	%br/

	%br/
	%br/

	= f.button :submit, class: 'btn btn-primary', id: 'photo-submit' do
		Subir foto