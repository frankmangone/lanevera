/* Success */

$("#new_photo").remove();
$("#new_product").append('<input class="form-control" type="hidden" name="product[photo_id]" id="product_photo">');
$("#product_photo").val("<%= @photo.id %>");

$("#photo-preview").css('background-image', 'url(<%= j @photo.photo.url() %>)');

// Remove upload info:

$icon = $("#photo-upload-info").find("h2").find("span")
$text = $("#photo-upload-info").find("p")

$icon.removeClass('glyphicon-time');
$text.text('');

// Enable upload button

$("#create-product").removeAttr('disabled');