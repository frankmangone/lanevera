/* Check if there are any errors */

/*if(<%= @product.errors.any? %>){
	alert(<%= @product.errors.empty? %>);
	var key;
	var value;

	<% @product.errors.each do |key, value| %>
		key = "<%= key %>";
		value = "<%= value %>";
		handleError('product', key, value);
	<% end %>
}*/

/* If there aren't, render the new tab */

//else {
	//alert("hola!");
	/*var $form = $("#new-product-form").parent();
	$form.before("<%= j render @product %>");
	var $product = $("#prod<%= @product.id %>");

	$form.slideUp( function(){
		$(this).remove();
	});

	productImageAutosize($product.find(".image-wrapper"));

	$product.hide().slideDown( function(){
		$("#new-product").slideDown();
	});*/

//}